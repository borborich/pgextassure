# Public corpus digest-bound scope follow-up — 2026-07-29

This follow-up applies Scope Plan `1.0` to the three conservative failures in
the 16-repository expansion. It is a scanner-completeness benchmark, not a
security ranking of the upstream projects.

## Reproduction

- manifest: `manifest-scope.tsv`;
- 16 detached checkouts at the exact recorded commits;
- regular report schema: `1.4`;
- grouped report schema: `1.3`;
- scope plan schema: `1.0`;
- no repository code was built, loaded, imported, or executed.

```bash
python tools/run_public_corpus.py /path/to/corpus \
  --manifest benchmarks/public-corpus/manifest-scope.tsv
```

## Result

- complete scans: **16/16**;
- fail-closed scan errors: **0**;
- analyzed files: **2,114**;
- findings: **915**;
- conservatively grouped root causes: **340**;
- generated artifacts declared by existing generation plans: **4**.

The three scope plans contain 50 exact exclusions:

| Repository | Exclusions | Former failure | Completed scan |
| --- | ---: | --- | ---: |
| `pg_stat_monitor` | 2 regular files | two SQL test-data files above 8 MiB | 37 files |
| `pgvectorscale` | 47 symlinks | version-chain SQL aliases | 62 files |
| `timescaledb` | 1 symlink directory | test utility alias | 1,188 files |

Every regular exclusion is pinned to its file bytes. Every symlink exclusion
is pinned to its link-target text and is never followed. The runner records
the exact plan digest in normalized output:

- `pg_stat_monitor`: `sha256:1cad92f25254a7f3cd8255a9f0554493e1c1dc7f9295d6d354ad6ae544a8944c`;
- `pgvectorscale`: `sha256:8125e80b819a1e2f0542678279fc342e2be87262232e48206c5ced861ac5b4d7`;
- `timescaledb`: `sha256:52c6b2d8749b6e2cdefb8da96e58f24a614f97b0e6c54f3585a6303c20796a4b`.

These plans do not make the global scanner permissive. An undeclared symlink,
digest mismatch, changed type, missing entry, or unused exclusion still fails
closed.
